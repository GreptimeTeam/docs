// This file intentionally left minimal.
// We don't need to wrap CodeBlock anymore because whitespace-safe copying
// is handled by the swizzled CopyButton in `src/theme/CodeBlock/CopyButton/index.tsx`.

import CodeBlock from '@theme-original/CodeBlock';

export default CodeBlock;
